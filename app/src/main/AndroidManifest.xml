<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.andromeda.movieado">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name="ir.andromeda.movieado.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CartoonAbad"
        android:usesCleartextTraffic="true">
        <activity
            android:name="ir.andromeda.movieado.feature.genre.GenreActivity"
            android:exported="false" />
        <activity
            android:name="ir.andromeda.movieado.feature.search.SearchActivity"
            android:exported="false" />
        <activity
            android:name="ir.andromeda.movieado.feature.list.ListActivity"
            android:exported="false" />
        <activity
            android:name="ir.andromeda.movieado.feature.detail.DetailMovieActivity"
            android:exported="false" />
        <activity
            android:name="ir.andromeda.movieado.feature.detail.DetailSeriesActivity"
            android:exported="false" />
        <activity
            android:name="ir.andromeda.movieado.feature.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="ir.andromeda.movieado.feature.player.PlayerActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="false" />
        <activity
            android:name="ir.andromeda.movieado.feature.main.MainActivity"
            android:exported="false" />
    </application>

</manifest>